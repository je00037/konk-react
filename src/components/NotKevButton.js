import '../css/App.css';

const NotKevButton = (props) => {
    return (
        <div>
            <button className="Button Button-red">Not Kev!</button>
        </div>
    )
}

export default NotKevButton;